# Local research notes

Per-backend notes in this directory are local and untracked. They may contain
time-sensitive observations about a developer's configured backends, so they are not
release artifacts or evidence. Recreate and verify them from provider documentation and
safe read-only observations when needed; do not commit credentials, personal data, local
paths, inventories, or tool-specific state.
